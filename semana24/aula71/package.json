{
   "scripts": {
      "array": "clear && tsnd --transpile-only --ignore-watch node_modules ./src/array/index.ts",
      "linkedList": "clear && tsnd --transpile-only --ignore-watch node_modules ./src/linked-list/index.ts",
      "stack": "clear && tsnd --transpile-only --ignore-watch node_modules ./src/stack/index.ts",
      "queue": "clear && tsnd --transpile-only --ignore-watch node_modules ./src/queue/index.ts"

   },
   "devDependencies": {
      "@types/node": "^15.3.0",
      "ts-node-dev": "^1.1.6",
      "typescript": "^4.2.4"
   }
}
