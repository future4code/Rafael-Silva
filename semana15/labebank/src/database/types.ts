

export type user = {
    name: string;
    birthDate: string;
    document: number;
    balance: number;
   
};

export type extract = {
    value: number;
    date: string;
    description: string;
};